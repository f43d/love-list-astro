# 2026-08-30 — PERMANENCE.md filled in

## What was done

Filled in the open placeholders in `docs/PERMANENCE.md` after a short Q&A:

- **Registrar / DNS**: Cloudflare (single email, no backup email set)
- **Domain renewal**: 6 June 2027, auto-renew on, paid by card
- **GitHub designated successor**: a son or daughter (name TBD when set up)
- **GitLab mirror**: to be created (one-line `git remote add gitlab …` + `git push`)
- **Local backup**: action item to do later (clone to Mac/external drive)

The doc was restructured slightly:

- Added an **"Open action items"** section at the very top so a future
  reader sees the unfinished work before the longer narrative. The
  four items are: (1) set the GitHub successor, (2) set a calendar
  reminder for 6 June 2027, (3) mirror to GitLab, (4) add a local
  backup.
- Added the **GitLab mirror** row to the "Where the site lives" table.
- Expanded the **Cloudflare account** section with a note that the
  account owns both DNS and registration (so a backup-registrar
  path doesn't exist here).
- Kept the existing "What to do if X breaks" sections.

No code changes. Doc-only commit.

## Open items (unchanged)

- The four action items at the top of `docs/PERMANENCE.md`
- 牆 強 啓 為 說 沒 氹 錄 — still missing from DC-CST
- Test the blessing form end-to-end
- Revoke the `…jdrra` PAT (it was used in this session)

---

# Late evening — handoff, AI-workflow docs, DC-CST final fixes

## What was done

### DC-CST missing-character substitutions (final round)

After the audit, the 10 characters DC-CST doesn't have were:
啟 強 氹 沒 涙 淚 為 牆 說 錄.

We substituted the four that were rendered in a DC-CST context
(bucket list body + footer):

- 強 → 强 (Simplified, in Footer.astro) — the owner's name still reads
  correctly because the display name lives in HTML text, not the
  font's own data.
- 氹 → 哄 (in data/list.env item 28) — "講鬼故哄你訓覺" reads more
  naturally in a parent-to-child register than 氹 (HK slang "trick").
- 為 → 爲 (in data/list.env item 74)
- 錄 → 録 (in data/list.env item 89)

The remaining six (牆, 啟, 為, 沒, 淚, 涙) don't render in a DC-CST
context anywhere in the live site — they're either in the comment page
which uses `--font-form` (Noto Sans HK), or the 100-reasons-why page
which uses `--font-prose` (wenwrite), or in a non-visual meta tag.
So they were never actually broken on the live site.

The user proposed 說/録 as substitutes for 說/錄. 說 (U+8AAA) is
in Noto Sans HK but not in DC-CST; 録 (U+9332) IS in DC-CST. The user
also proposed 爲 (U+7232) as a Traditional alternative to 為, which
is in DC-CST and was used for item 74.

After this round, no DC-CST-missing char renders in a DC-CST context.

### Handoff doc (`docs/HANDOFF.md`)

Wrote a self-contained brief for any future AI session on this project.
Contains:

- What this project is + the tech stack with reasoning
- The recommended read order (AGENTS.md, ARCHITECTURE.md, etc.)
- Repo conventions (TS strict, CSS tokens, data file format, end-of-day routine)
- Current state at handoff: what works, what's still to do
- The exact prompt the user will paste to start a fresh session
- The end-of-day routine the user triggers by saying "call it a day"
- A "what NOT to do" list to prevent re-arguing settled questions
- Quick reference for repo info, env vars, hidden pages

### AI workflow doc (`docs/AI-WORKFLOW.md`)

Captured the conclusion from the day-1 "oh-my-code / superpowers" question:
when to install which opencode plugin, and when to install nothing.
For this site: nothing (the conventions in AGENTS.md are enough). For a
new real codebase: obra/superpowers. For one-shot big refactors:
oh-my-opencode-slim temporarily. Linked from PERMANENCE.md so future
sessions find it.

### Final session state

- 3 doc commits in this late session (HANDOFF, AI-WORKFLOW, end-of-day
  append).
- 2 text-fix commits (強/氹 + 為/錄).
- 0 code changes this session.
- The handoff doc is the user's anchor for starting fresh sessions
  with no loss of continuity.

## Open items (unchanged from earlier today)

- 4 action items at the top of `docs/PERMANENCE.md` (set GitHub
  successor, set calendar reminder, set up GitLab mirror, add local
  backup).
- 牆 強 啓 為 說 沒 氹 錄 — all the previously-missing DC-CST chars are
  now resolved (either substituted or rendered via Noto Sans HK /
  wenwrite).
- Test the blessing form end-to-end (Web3Forms key is set; owner
  needs to actually test).
- Revoke the `…jdrra` PAT (one-time end-of-session reminder).

---

# Evening — OpenCode web UI setup + handoff for next session

## What was done (outside the project repo)

- **Decided to use OpenCode web UI** for the next chat session
  instead of the CLI. Reason: the user has an iPhone and wants to share
  screenshots, files, and chat history without SSH-fumbling. Web UI
  gives a native iPhone-Safari experience.
- **Set up the web UI on the Proxmox LXC**:
  - Installed / located the `opencode` binary at `/root/.opencode/bin/opencode`.
  - Found the right flag set: `opencode web --port 3000 --hostname 0.0.0.0 --pure`.
  - Set up a systemd service (`/etc/systemd/system/opencode-web.service`)
    with auto-restart and an `OPENCODE_SERVER_PASSWORD` env var.
  - The `--pure` flag tells OpenCode to skip its hosted-service
    billing checks; the user's MiniMax M3 API key is what matters for
    the AI model, not OpenCode's own subscription tier.
  - Final URL: `http://192.168.1.40:3000` (LXC's LAN IP).
- **Wrote the start-prompt infrastructure for the next session**:
  - `docs/HANDOFF.md` — full project brief that a fresh session can
    read in 2-3 minutes and be productive.
  - `docs/AI-WORKFLOW.md` — when to install which opencode plugin
    (current site: nothing; new codebases: obra/superpowers; big
    refactors: oh-my-opencode-slim temporarily).
  - Updated `docs/PERMANENCE.md` to link to both new docs.

## Repository state at handoff

- **Last commit**: `e77be55` (docs: end-of-day 2026-08-30 late session).
- **Web UI is live** at `http://192.168.1.40:3000` (LXC LAN IP).
- **Project code is clean**:
  - `npx astro check` → 0 errors, 0 warnings.
  - `npm run build` → 5 pages, builds clean.
  - All 10 previously-missing DC-CST characters are now resolved.
- **Open items at top of `docs/PERMANENCE.md`** (action items):
  1. Set a GitHub designated successor (son/daughter)
  2. Set calendar reminder for 6 June 2027 (domain renewal)
  3. Set up GitLab mirror
  4. Add a local backup

## How the user starts the next session

The user will open the web UI at `http://192.168.1.40:3000` and paste
this prompt (also captured in `docs/HANDOFF.md`):

> Read these files in this order, then summarise back to me what
> you know about this project before doing anything. The repo is
> f43d/love-list-astro (rooted at the current directory — confirm
> with `git remote -v`):
> 1. AGENTS.md
> 2. docs/ARCHITECTURE.md
> 3. docs/DECISIONS.md
> 4. docs/OPERATIONS.md
> 5. docs/PERMANENCE.md
> 6. docs/HANDOFF.md
> 7. docs/AI-WORKFLOW.md
> 8. git log --oneline -20
> 9. docs/sessions/2026-08-30.md
>
> Today's task: [whatever you want done]

## Known caveats for the next session

- **Token budget is shared, not reset.** The MiniMax Max Coding plan
  limits apply per account, not per session. The next session has no
  memory of the prior chat, but it draws from the same monthly token
  pool. Reusing the start prompt above is fine; the 9 files will be
  read once and the cache will hit them on subsequent reads.
- **Per the `AGENTS.md` rule**: don't nag the user about PAT
  revocation at session end (this reminder is the one allowed
  instance). The PAT used in this session was `...jdrra`, stored on
  the LXC's git remote only briefly. Recommend user revoke it
  if they haven't already.
