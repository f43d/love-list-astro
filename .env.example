.example

# Public (browser-readable) environment variables. Prefix with PUBLIC_ so Astro
# exposes them in client code.

# Web3Forms access key — get yours at https://web3forms.com (free, no signup beyond email).
# The key is intentionally public (it goes into the form HTML), but rotate if abused.
PUBLIC_WEB3FORMS_KEY=
